# Tina Cloud — get these from https://app.tina.io
TINA_PUBLIC_CLIENT_ID=your-client-id
VITE_TINA_CLIENT_ID=your-client-id

# Read-only token for production builds and GraphQL API (keep secret)
TINA_TOKEN=your-read-token

# Optional: override git branch Tina syncs with
# TINA_BRANCH=main
